describe('' , () => {
  it('ll', () => {
    for(let index = 0; index < [times]; index++) {
      cy.url().should('be.exist')
      cy.visit('')
      for(let index = 0; index < [times]; index++) {
      }
    }
  })
  it('', () => {
    cy.url().should('be.exist')
  })
})